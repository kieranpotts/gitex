#!/bin/env sh
set -eu

#
# git-configure - Open `~/.gitconfig` in your default text editor.
#
# Usage:
#   $ git configure
#
# Dependencies: None
#

main() {
  if [ $# -gt 0 ]; then
    echo "error: git-configure does not accept any arguments" >&2
    return 1
  fi

  # ...
}

main "$@"
