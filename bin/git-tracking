#!/bin/env sh
set -eu

#
# git-tracking - Same as `git branches` but also shows the names of
# the tracked upstream branches.
#
# Usage:
#   $ git tracking
#
# Dependencies: None
#

main() {
  if [ $# -gt 0 ]; then
    echo "error: git-tracking does not accept any arguments" >&2
    return 1
  fi

  # ...
}

main "$@"
