#!/bin/env sh
set -eu

#
# git-staged - List files with changes staged for committing.
#
# Usage:
#   $ git staged
#
# Dependencies: None
#

main() {
  if [ $# -gt 0 ]; then
    echo "error: git-staged does not accept any arguments" >&2
    return 1
  fi

  # git diff --name-only --cached
}

main "$@"
