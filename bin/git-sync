#!/bin/env sh
set -eu

#
# git-sync - Sync the local checked-out branch with its tracked
# upstream branch, maintaining linear history.
#
# Usage:
#   $ git sync
#
# Dependencies: None
#

main() {
  if [ $# -gt 0 ]; then
    echo "error: git-sync does not accept any arguments" >&2
    return 1
  fi

  # ...
}

main "$@"
