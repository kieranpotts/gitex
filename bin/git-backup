#!/bin/env sh
set -eu

#
# git-backup - Create a backup of the current branch or another
# specified branch.
#
# Usage:
#   $ git backup ([branch])
#
# Dependencies: None
#

main() {
  if [ $# -gt 0 ]; then
    echo "error: git-backup does not accept any arguments" >&2
    return 1
  fi

  # ...
}

main "$@"
