#!/bin/env sh
set -eu

#
# git-current - Show the name of the current branch.
#
# Usage:
#   $ git current
#
# Dependencies: None
#

main() {
  if [ $# -gt 0 ]; then
    echo "error: git-current does not accept any arguments" >&2
    return 1
  fi

  # git rev-parse --abbrev-ref HEAD
}

main "$@"
