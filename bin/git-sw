#!/bin/env sh
set -eu

#
# git-sw - Shortcut for `git switch`. Options are forwarded.
#
# Usage:
#   $ git sw [...]
#
# Dependencies: (none)
#

main() {
  # Forward all options to git switch.
  git switch "$@"
}

main "$@"
