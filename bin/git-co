#!/bin/env sh
set -eu

#
# git-co - Shortcut for `git checkout`. Options are forwarded.
#
# Usage:
#   $ git co [...]
#
# Dependencies: None
#

main() {
  # Forward all options to git checkout.
  git checkout "$@"
}

main "$@"
