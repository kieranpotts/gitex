#!/bin/env sh
set -eu

#
# git-track - Start tracking a new file. This is an alias for
# `git add` and the opposite of the `git untrack` alias.
#
# Usage:
#   $ git track [path]
#
# Dependencies: None
#

main() {
  if [ $# -gt 0 ]; then
    echo "error: git-whoami does not accept any arguments" >&2
    return 1
  fi

  # ...
}

main "$@"
