#!/bin/env sh
set -eu

#
# git-delete - Delete a local branch, but only if it's changes have
# been merged elsewhere.
#
# Usage:
#   $ git delete ([branch])
#
# Dependencies: None
#

main() {
  if [ $# -gt 0 ]; then
    echo "error: git-whoami does not accept any arguments" >&2
    return 1
  fi

  # ...
}

main "$@"
