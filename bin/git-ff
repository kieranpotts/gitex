#!/bin/env sh
set -eu

#
# git-ff - Perform a fast-forward merge.
#
# Usage:
#   $ git ff ([branch])
#
# Dependencies: None
#

main() {
  if [ $# -gt 0 ]; then
    echo "error: git-whoami does not accept any arguments" >&2
    return 1
  fi

  # branch=${1:-main}

  # git checkout "$branch"
  # git pull --ff-only
}

main "$@"
