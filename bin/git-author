#!/bin/env sh
set -eu

#
# git-author - Change the author of the last commit or an earlier
# specified commit.
#
# Usage:
#   $ git author ([ref])
#
# Dependencies: None
#

main() {
  if [ $# -gt 0 ]; then
    echo "error: git-whoami does not accept any arguments" >&2
    return 1
  fi

  # ...
}

main "$@"
