#!/bin/env sh
set -eu

#
# git-ignored - List all ignored files.
#
# Usage:
#   $ git ignored
#
# Dependencies: None
#

main() {
  if [ $# -gt 0 ]; then
    echo "error: git-ignored does not accept any arguments" >&2
    return 1
  fi

  # git ls-files --ignored --exclude-standard
}

main "$@"
