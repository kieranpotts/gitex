#!/bin/env sh
set -eu

#
# git-cl - Shortcut for `git clone`. Options are forwarded.
#
# Usage:
#   $ git cl [...]
#
# Dependencies: None
#

main() {
  # Forward all options to git clone.
  git clone "$@"
}

main "$@"
