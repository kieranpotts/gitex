#!/bin/env sh
set -eu

#
# git-cl - Shortcut for `git clone`. Parameters are forwarded.
#
# Usage:
#   $ git cl ([params...])
#
# Dependencies: None
#

main() {
  if [ $# -gt 0 ]; then
    echo "error: git-cl does not accept any arguments" >&2
    return 1
  fi

  # ...
}

main "$@"
