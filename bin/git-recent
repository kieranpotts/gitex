#!/bin/env sh
set -eu

#
# git-recent - Clean `git log`, last 25 entries only.
#
# Usage:
#   $ git recent
#
# Dependencies: None
#

main() {
  if [ $# -gt 0 ]; then
    echo "error: git-whoami does not accept any arguments" >&2
    return 1
  fi

  # ...
}

main "$@"
