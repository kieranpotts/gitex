#!/bin/env sh
set -eu

#
# git-orphan - Create an orphaned branch and switch to it. Working
# changes will be added to the staging index, unless the `--fresh`
# option is provided, in which case the working directory will be
# cleared – careful, you may lose work-in-progress.
#
# Usage:
#   $ git orphan [branch] (--fresh)
#
# Dependencies: None
#

main() {
  if [ $# -gt 0 ]; then
    echo "error: git-whoami does not accept any arguments" >&2
    return 1
  fi

  # ...
}

main "$@"
