#!/bin/env sh
set -eu

#
# git-untrack - Untrack a specified file.
#
# Usage:
#   $ git untrack [path]
#
# Dependencies: None
#

main() {
  if [ $# -gt 0 ]; then
    echo "error: git-untrack does not accept any arguments" >&2
    return 1
  fi

  # ...
}

main "$@"
