#!/bin/env sh
set -eu

#
# git-unignore - Stop ignoring changes made to a file.
#
# Usage:
#   $ git unignore [path]
#
# Dependencies: None
#

main() {
  if [ $# -gt 0 ]; then
    echo "error: git-unignore does not accept any arguments" >&2
    return 1
  fi

  # ...
}

main "$@"
