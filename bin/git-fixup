#!/bin/env sh
set -eu

#
# git-fixup - Fix something in the most recent commit or an earlier
# specified commit. Rewrites history.
#
# Usage:
#   $ git fixup ([ref])
#
# Dependencies: None
#

main() {
  if [ $# -gt 0 ]; then
    echo "error: git-fixup does not accept any arguments" >&2
    return 1
  fi

  # ...
}

main "$@"
