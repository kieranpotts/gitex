#!/bin/env sh
set -eu

#
# git-uncommit - Undo the last commit, returning the changes
# introduced in the commit to the staging index.
#
# Usage:
#   $ git uncommit
#
# Dependencies: None
#

main() {
  if [ $# -gt 0 ]; then
    echo "error: git-whoami does not accept any arguments" >&2
    return 1
  fi

  # ...
}

main "$@"
