#!/bin/env sh
set -eu

#
# git-remotes - List the URLs of all remotes.
#
# Usage:
#   $ git remotes
#
# Dependencies: None
#

# shellcheck source=../lib/print.sh
. "$(dirname "$0")/../lib/print.sh"

main() {
  if [ $# -gt 0 ]; then
    echo "error: git-remotes does not accept any arguments" >&2
    return 1
  fi

  # git remote -v
}

main "$@"
