#!/bin/env sh
set -eu

#
# git-default - Show the name of the default branch, as configured in
# the "origin" repo.
#
# Usage:
#   $ git default
#
# Dependencies: None
#

main() {
  if [ $# -gt 0 ]; then
    echo "error: git-default does not accept any arguments" >&2
    return 1
  fi

  # git rev-parse --abbrev-ref --symbolic-full-name origin/HEAD | sed 's@^origin/@@'
}

main "$@"
