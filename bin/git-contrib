#!/bin/env sh
set -eu

#
# git-contrib - List all contributors, ordered by their total commit count.
#
# Usage:
#   $ git contrib
#
# Dependencies: None
#

main() {
  if [ $# -gt 0 ]; then
    echo "error: git-contrib does not accept any arguments" >&2
    return 1
  fi

  # ...
}

main "$@"
