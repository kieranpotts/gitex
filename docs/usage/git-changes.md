# `git changes`

Show the changes introduced in a specified commit, or introduced to the working tree plus staging area since the last commit (ie. the working, uncommitted changes).

## Usage

```
$ git changes [<ref>]
```
