= `git orphan [branch] (--fresh)`

Create an orphaned branch and switch to it. Working changes will be added to the staging index, unless the `--fresh` option is provided, in which case the working tree will be cleared – careful, you may lose work-in-progress.
